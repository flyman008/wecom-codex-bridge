{
  "name": "wecom-agent-router",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "企业微信智能机器人长连接与本地 Agent 路由服务",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/src/index.js",
    "start": "node dist/src/index.js",
    "setup": "node scripts/setup.mjs",
    "autostart:apply": "node scripts/apply-autostart.mjs",
    "doctor": "node scripts/doctor.mjs",
    "check:codex": "npm run build && node scripts/check-codex.mjs",
    "check:model": "node scripts/check-llm.mjs",
    "check:router": "npm run build && node scripts/check-router.mjs",
    "test": "node --test dist/test/*.test.js test/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "@wecom/aibot-node-sdk": "1.0.6",
    "@wecom/cli": "1.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
